import { Lora } from "next/font/google";

const lora = Lora({
  subsets: ['latin'],
  weight: ['400']
});

const Skill = () => {
    return (
        <div className={lora.className}>
        
        </div>
    );
}
 
export default Skill;